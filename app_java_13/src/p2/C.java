package p2;

import p1.A;

public class C extends A {//here class A is default class so we can't access anything
	//in different package
    // if we make class Public then we can access it from anywhere.
}
